import React from 'react';
import { View } from 'react-native';
import { Map } from '../components';

export const Maps = () => {
  return (
    <View style={{ flex: 1 }}>
      <Map />
    </View>
  );
};
